@model Project3_FinalExam.ViewModels.UndergradViewModel

@section scri {
    <script src="~/materialize/materialize.css"></script>
    <script src="~/materialize/materialize.min.css"></script>
    <script src="~/jqueryui/jquery-ui.css"></script>
    
}

@section CSS {
    <style>
        body {
            background-color: bisque;
        }
        .card.medium {
            width: 300px;
            height: 400px;
        }
        h4{
            text-align:center;
            color: white;
        }
        h2{
            text-align: center;
        }
        #desc{
            text-align:center;
        }
        .card{
            background-color:darkorange;
            
        }
        .card-content{
            padding-top: 10px;
        }
    </style>
}

<h2>@Model.Title</h2>
<div class="row">
    @foreach (var grad in Model.UnderGrads)
    {
    
<div class="col s12 m6 l4">
    <div class="card medium">
       
        <div class="card-content">
            <h4 style="background-color:gray"> @grad.title</h4>
            <p id="desc"> @grad.description</p>
            <p><b>Concentrations:</b></p>
            <ul>
                @foreach (var con in grad.concentrations)
                {
                <li> @con</li>
                }
            </ul>
        </div>
    </div>
</div>            }
        
</div>

    @*<div id="myUnderDegrees"></div>

    @foreach (var under in Model.UnderGrads)
    {
<h3 id=@String.Concat("myHdng", under.degreeName)><b>@under.degreeName</b></h3>

                <div id=@String.Concat("myDiv", under.degreeName)>
                    <p>@under.title</p>
                    <p>@under.description</p>
                    <ul>
                        @foreach (var conc in @under.concentrations)
                        {
        <li>@conc</li>
}
                    </ul>
                </div>}*@
    @*Get jQuery and jQueryUI Scripts from wwwroot*@
    @section Scripts {
        <script src="~/jquery/jquery.js"></script>
        <link rel="stylesheet" href="~/jqueryui/themes/le-frog/jquery-ui.min.css">

        <script src="~/jqueryui/jquery-ui.js"></script>
        @*<script>// Create Accordion
            let headings = document.querySelectorAll("h3");
            let divs = document.querySelectorAll("[id^='myDiv']");

            for (var i = 0; i < headings.length; i++) {
                $("#myUnderDegrees").append($("#" + headings[i].id));
                $("#myUnderDegrees").append($("#" + divs[i].id));
            }

            $('#myUnderDegrees').accordion({
                collapsible: true,
                active: false,
                heightStyle: "content"
            });</script>*@
    }